import React from 'react';
import PropTypes from 'prop-types'

const propTypes = {};

const defaultProps = {};

const Week = props => (
  <div>
    Hey, this is the week view stub.
  </div>
);

Week.propTypes = propTypes;

Week.defaultProps = defaultProps;

export default Week;
