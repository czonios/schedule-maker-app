import React from 'react';
import PropTypes from 'prop-types'

const propTypes = {};

const defaultProps = {};

const Day = props => (
  <div>
    Hey, this is the day view stub
  </div>
);

Day.propTypes = propTypes;

Day.defaultProps = defaultProps;

export default Day;
